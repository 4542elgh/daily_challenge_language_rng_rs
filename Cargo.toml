[package]
name = "daily_challenge_language_rng" # the name of the package
version = "0.1.0"    # the current version, obeying semver
authors = ["Evan <4542elgh@gmail.com>"]

[dependencies]
rand = "0.7"
json = "0.12.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
